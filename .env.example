# MongoDB connection
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/
MONGODB_DB_NAME=streamjacking_detector
MONGODB_COLLECTION_NAME=detected_videos

# JWT auth (generate a strong random secret, e.g.: openssl rand -hex 32)
JWT_SECRET=your-secret-key-here
JWT_EXPIRES_IN=7d
